name: main_sdk
description: Shared SDK for Selekt applications.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.10.4

dependencies:
  dio: ^5.4.0
  flutter:
    sdk: flutter
  url_launcher: ^6.3.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
